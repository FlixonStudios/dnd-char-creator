import React from 'react';

function SelectionController({attributeUnder, masterData, noOfChoices,
                                 categoryToSend, setFunction, optionKey,
                                    optionFromKey='from', optionChooseKey='choose'
}) {


    return (
        <div></div>
    );
}

export default SelectionController;